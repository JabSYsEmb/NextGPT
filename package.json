{
  "name": "svelty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:mkdir": "mkdir -p dist dist/src dist/service-worker",
    "copy:manifest": "cp ./static/manifest.json dist/manifest.json",
    "copy:service-worker": "cp ./static/service-worker/bg.js dist/service-worker/bg.js",
    "copy:scripts": "cp -Rf ./static/scripts/* dist/src/",
    "build:copy": "npm run build:mkdir && npm run copy:manifest && npm run copy:service-worker && npm run copy:scripts",
    "build:svelte": "rollup -c",
    "build:dev": "npm run build:copy && npm run build:svelte",
    "build:watch": "nodemon",
    "build": "npm run build:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^28.0.1",
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^12.1.1",
    "chrome-types": "^0.1.312",
    "rollup": "^4.24.0",
    "rollup-plugin-svelte": "^7.2.2"
  },
  "dependencies": {
    "svelte": "4"
  }
}
